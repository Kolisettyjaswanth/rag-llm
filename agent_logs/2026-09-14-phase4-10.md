# Phase 4-10 implementation log

## Starting state
The repository already had PostgreSQL/pgvector RAG, Ollama, a React chat UI, and partial agent files. The existing agent was wired into `/api/chat`, but the frontend did not display route metadata or artifacts and there were no agent tests or assessment documents.

## Failed attempt and correction
- Attempt: ran the new agent test before rebuilding the Docker backend image.
- Result: `tests/test_agent.py` was not found inside the container because the image still contained the previous test tree.
- Correction: rebuilt only the backend image with `docker compose up -d --build backend`, then reran the focused tests successfully.

## Implemented
- Added dependency injection to `GrowthAgent` for cheap deterministic tests.
- Added deterministic routing coverage for RAG, Ship30, and artifact requests.
- Added optional Claude Agent SDK provider without changing the Ollama default.
- Added artifact metadata and sandboxed iframe rendering to the frontend.
- Added artifact sanitization coverage.
- Added API response-shape coverage and fixed the rollback test to target `growth_agent`.
- Added PRD, design, and architecture documentation.

## P0/P1 correction entry

- Problem: Ollama sometimes returned HTML inside Markdown fences, so the artifact iframe displayed source text instead of a webpage.
- Correction: normalized optional HTML/plain Markdown fences before defense-in-depth sanitization and added tests for fenced, plain, CSS, and active-content cases.
- Architecture concern: the Claude SDK was previously only an optional provider while `GrowthAgent` was custom.
- Decision: added `ClaudeAgentExecutor` under `app/agent/`; `GrowthAgent` selects it when Claude mode is configured, while Ollama remains the default local path. The compatibility provider delegates to the same executor.
- Added controlled pgvector retrieval-contract tests and a manual UI test plan.

## Validation notes
No credentials, tokens, passwords, or transcript contents were written to this log. The transcript dataset remains ignored by Git.
